trigger:
- main

pool:
  vmImage: 'ubuntu-latest'

steps:
- task: Docker@2
  inputs:
    containerRegistry: 'docker-hub-connection'
    repository: 'binakakumari/myapi'
    command: 'buildAndPush'
    Dockerfile: '**/Dockerfile'
    tags: 'latest'